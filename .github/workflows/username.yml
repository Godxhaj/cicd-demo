name: Echo Username

on:
  push:

jobs:
  say-username:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
	uses: actions/checkout@v3

      - name: echo github username
	run: echo" your github name is : ${{ github.actor }}"
